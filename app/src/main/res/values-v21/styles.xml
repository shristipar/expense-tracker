<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <item name="android:statusBarColor">@color/colorPrimaryText</item>
    </style>

    <!-- Date Picker Theme Style -->
    <style name="MyDatePickerTheme">
        <item name="colorControlActivated">@color/black</item>
    </style>
    <style name="MyDatePickerStyle" parent="@android:style/Widget.DatePicker">
        <item name="android:headerBackground">@color/white</item>
        <item name="android:calendarTextColor">@color/white</item>

        <item name="android:dayOfWeekBackground">@color/white</item>
        <!--<item name="android:dayOfWeekTextAppearance">@color/white</item>-->

        <item name="android:yearListSelectorColor">@color/white</item>
        <item name="android:datePickerMode">calendar</item>

        <item name="android:colorAccent">@color/black</item>
        <item name="colorAccent">@color/black</item>
        <item name="android:focusedMonthDateColor">@color/black</item>
    </style>

</resources>
